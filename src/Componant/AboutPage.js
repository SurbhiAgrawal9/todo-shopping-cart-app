import React from 'react';

const AboutPage = () => {
    return (
        <div>
            <h2>About</h2>
            <p>This is a React application built as a simple to-do list and shopping cart.</p>
            <p>Feel free to explore and use the app!</p>
        </div>
    );
};

export default AboutPage;
